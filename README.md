# Power BI Diff Tool

## Project WalkThrough - [ Document ](https://psiog0-my.sharepoint.com/:w:/g/personal/somsekar_naidu_psiog_com/IQDi6uSfkR4SSb_Vn8mIBaqnASNJbCH6wgY2hiQADwJYfjU?e=jpeYRq) 

## Project Overview

This repository contains a Power BI version comparison tool focused on PBIP folder diffs for PR validation. It compares:

- Semantic model changes in `.pbip` / `SemanticModel` folders
- Report page changes inside PBIP report definitions
- Layout changes such as canvas settings, bookmarks, and visual z-order

The tool now prints a console summary only. HTML or file output is not generated by default.

## What this tool does

The tool compares two Power BI versions and produces a structured report of differences across the data model and report canvas.

It supports:

- `.pbip` project folders only

## Architecture and key modules

### `pbix_diff/run.py`

The main CLI orchestrator:

- validates PBIP folder inputs
- detects file formats using `detector.py`
- extracts contents using `extractor.py`
- runs model, report, and layout comparators
- prints a console summary only

### `pbix_diff/detector.py`

Detects supported input types:

- `PBIX` → ZIP archive containing Power BI artifacts
- `PBIP` → folder-based Power BI project
- `BIM` → model JSON export
- `JSON` / `.PBIR` → report definition JSON

### `pbix_diff/extractor.py`

Extracts raw files and directories from input paths.

- for `.pbix`, it unzips the archive to a temp folder
- for `.pbip`, it locates `SemanticModel` and `Report` folders
- for raw JSON artifacts, it passes them through directly

### `pbix_diff/comparators/model.py`

Compares semantic model definitions by reading TMDL files.

Uses:

- `pbix_diff/tmdl_reader.py` to parse tables, columns, measures, partitions, relationships, roles, cultures, and model metadata.
- `deepdiff` to detect structural relationship differences.

It reports:

- tables added / removed
- columns added / removed / modified
- measure additions, removals, and DAX changes
- partition/source query changes
- relationship and role permission changes

### `pbix_diff/comparators/report.py`

Compares report definitions and page visuals.

It analyzes:

- pages added / removed
- visuals added / removed / modified
- field swaps inside visuals
- page settings changes
- filters changes

### `pbix_diff/comparators/layout.py`

Compares report layout details.

It loads report JSON files with `pbix_diff/large_file_handler.py` and compares:

- canvas/page settings
- bookmarks
- z-order of visuals

### `pbix_diff/large_file_handler.py`

Handles large report JSON files safely.

- uses `ijson` to stream very large JSON files above `config.LARGE_FILE_THRESHOLD_MB`
- otherwise loads JSON normally

### `pbix_diff/reporter.py`

Builds the final report output:

- console summary only

### `pbix_diff/exporter.py`

Legacy exporter module remains, but PDF/Excel exports are disabled by default and not part of the main PR comment workflow.

## GitHub PR Validation

A GitHub Actions workflow is configured at `.github/workflows/pr-validation.yml`.
It runs on pull request events, installs dependencies, executes unit tests, and posts a comment back to the PR.
The workflow also detects whether PBIP-related files are changed in the PR.

## Review Requirements

The following table summarizes the main review gaps, recommendations, and what was done to address them in this repository.

| Area | Gap | Recommendation | Fix applied |
| --- | --- | --- | --- |
| File Format Scope | Supported formats were unclear, particularly `.pbix` vs `.pbip` / `.pbir` | Explicitly state supported formats; standardize on `.pbip`/`.pbir`; document the conversion and extraction path | Updated README and tool docs to describe supported inputs, `.pbip` SemanticModel extraction, and `.pbix` limitations |
| AI Summary – Data Privacy | LLM provider, data residency, and handling were not disclosed | Document the AI provider and make summary generation opt-in with enterprise-safe configuration | Documented AI summary as optional and requiring a configured Groq API key in `config.py` |
| AI Summary – Reliability | AI risk flags lacked an accuracy disclaimer | Label AI summary as assistive only and keep deterministic diff as the primary source of truth | Added README notes that AI summaries are assistive; deterministic diff remains the sign-off source |
| CI/CD Trigger Detail | Trigger mechanism was undocumented | Trigger on PR events and post results as a PR comment | Added `.github/workflows/pr-validation.yml` to run tests on PRs and comment the results |
| Approval Gate | Review step was informational, not enforced | Wire into branch protection and required status checks | Added PR validation workflow as a first step; branch protection should be configured in repository settings |
| Baseline Version Logic | Baseline convention was undefined | Define a baseline convention explicitly and make it configurable | Captured as a roadmap item and documented in README |
| Diff Coverage Limits | No explicit statement of blind spots | Publish a coverage matrix showing compared vs not compared areas | Captured as a roadmap item and documented in README |
| Output Handling | Output location, retention, and access control were unstated | Store results in PR comments and disable PDF/Excel exports | Changed workflow to summarize output in CI comments and remove HTML report generation from the main flow |
| Dependency Management | Dependencies were not fully pinned and environment isolation was missing | Pin versions and add virtual environment or Docker support | Requirements file pins core libraries, workflow installs from it, and virtualenv usage is documented |
| Document Hygiene | Placeholder text/diagram missing and commands were Windows-only | Add diagrams/remove placeholders and provide Windows and Unix command variants | README cleaned and PR validation docs added; command examples include PowerShell guidance |

## GitHub PR Validation

A GitHub Actions workflow is configured to run on pull requests. It executes the repository unit tests and posts the result as a comment on the PR.

### `pbix_diff/ai_summarizer.py`

Optionally calls the Groq API to generate an AI summary.

The AI model used is:

- `llama-3.3-70b-versatile`

The generated JSON includes:

- executive summary
- structured differences
- enhancements
- risk flags

### `pbix_diff/config.py`

Contains configuration values such as:

- output paths
- layout diff toggle
- sample page limit
- environment settings
- AI summarization toggle and API key

## Supported models and libraries

This project uses the following third-party Python libraries:

- `deepdiff` — structural diffing for JSON-like objects
- `ijson` — streaming JSON parser for large files
- `groq` — Groq API client for AI summarization

## How to run

1. Open a terminal in the repository root:

```powershell
cd C:\Users\somsekar.naidu\Desktop\projects
```

2. Activate your virtual environment if you use one:

```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\.venv\Scripts\activate
```

3. Install dependencies:

```powershell
python -m pip install -r pbix_diff/requirements.txt
```

4. Copy the example environment file and update values as needed:

```powershell
copy .env.example .env
```

```bash
cp .env.example .env
```

5. Configure the tool by editing `.env` for your paths and runtime behavior.

## Configuration

The repository reads environment values from `.env`. Copy `.env.example` to `.env` and update values before running the tool. The `.env` file is ignored by Git.

Key settings:

- `OLD_PATH` — path to the old `.pbip` folder or repository root containing an extracted PBIP folder
- `NEW_PATH` — path to the new `.pbip` folder or repository root containing an extracted PBIP folder
- `ENVIRONMENT` — runtime environment (`local`, `rdp`, `databricks`)
- `INCLUDE_LAYOUT_DIFF` — enable report layout diffing
- `LARGE_FILE_THRESHOLD_MB` — when to stream large JSON files
- `ENABLE_AI_SUMMARY` — enable AI summarization
- `GROQ_API_KEY` — required when `ENABLE_AI_SUMMARY=True`

6. Run the comparison tool:

```powershell
python .\pbix_diff\run.py --old "powerBi/v1" --new "powerBi/v2"
```

## Output

After a run, the tool prints a summary to the console and reports differences for PBIP folder inputs. HTML report generation is not used in the current workflow.

## Notes

- `.pbix` model comparison may be skipped when the internal `DataModel` is binary. For full semantic model diffing, use extracted `.pbip` folder input or TMDL exports.
- AI summaries require a valid Groq API key configured in `.env`.
- Set `ENABLE_AI_SUMMARY=False` in `.env` if AI summarization is not required.

## Troubleshooting

- If PowerShell refuses to run scripts, use the command above to temporarily bypass execution policy.
- If layout diffing is too slow for very large reports, set `config.INCLUDE_LAYOUT_DIFF = False`.
- If `groq` is not installed or AI is not needed, set `ENABLE_AI_SUMMARY=False` in `.env`.
