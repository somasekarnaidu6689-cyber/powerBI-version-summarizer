name: Build and Push PBIP Diff Image

on:
  push:
    branches:
      - main
      - main-v2
    paths:
      - 'pbix_diff/**'
      - 'Dockerfile'
  workflow_dispatch:        # ← add this so you can trigger it manually right now

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          no-cache: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/pbip-diff:latest
            ghcr.io/${{ github.repository_owner }}/pbip-diff:${{ github.ref_name }}